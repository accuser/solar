# Secrets / local config - *.env.example files are intentionally tracked
*.env

# Vendor docs dropped in the repo root for reference, not for tracking
*.pdf

# Full Node-RED export - contains live credentials (e.g. Hue bridge API key)
# for the user's whole home-automation instance, not just the parts this
# project integrates with. Kept locally for reference only.
node-red.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store

# Editors
.vscode/
.idea/
